[project]
name = "slice-workflow-cli"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
slice-workflow = "slice_workflow_cli.cli:main"

[build-system]
requires = ["uv_build>=0.9.0,<0.10.0"]
build-backend = "uv_build"
